services:
  redis:
    container_name: redis
    image: redis:latest
    networks:
      - jd_network

  fastapi:
    container_name: fastapi-redis-cicd
    build: .
    networks:
      - jd_network
    ports:
      - "8000:8000"
    depends_on:
      - redis

networks:
  jd_network: